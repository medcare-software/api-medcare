/**
 * Wrapper local (tsx). Em produção use:
 *   npm run script:backfill-store-downloads -- --days=90
 *
 * @deprecated Prefer `src/scripts/backfill-store-downloads.ts` (compila no dist da imagem).
 */
import '../src/scripts/backfill-store-downloads.js'
